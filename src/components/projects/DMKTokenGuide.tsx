import { Button, Stack } from "@mui/material";
import { Visibility as ViewIcon } from "@mui/icons-material";

export const DMKTokenGuide = () => {
  return (
    <Stack spacing={2}>
      <Button>
        <ViewIcon /> View it Live
      </Button>
    </Stack>
  );
};
